services:
  web:
    image: ${WEB_IMAGE:-ghcr.io/chrisyerga/newtricks-abbot:latest}
    container_name: abbot-web
    restart: unless-stopped
    networks:
      - porch
networks:
  porch:
    external: true
